const myPlugin = {
    install(app, options) {
      // configure the app
    }
  }

/*

Usage with a component:

import { createApp } from 'vue'

const app = createApp({})

app.use(myPlugin, {
  
})

*/